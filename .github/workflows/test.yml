name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes libportaudio2

      - name: Install dependencies
        run: uv sync --group dev

      - name: Run tests
        run: uv run pytest tests/ -v

  macos-swift-test:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4

      - name: Run Swift tests
        run: swift test --package-path macos/ZenWhisper

  windows-postprocessor-test:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python 3.12
        run: uv python install 3.12

      - name: Run Windows postprocessor tests
        run: >-
          uv run --no-project --with pytest --with tomli-w --with numpy
          --with pywin32
          python -m pytest tests/test_postprocessing.py -q
